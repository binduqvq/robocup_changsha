# MPE2 协同覆盖选拔赛：赛题与规程

版本：v1.2（2026-09-22 更新，取代 2026-09-22 的 v1.1 版本）。

本文分两部分：**第一部分**是比赛组织规程（编号目录、报分、提交、核验、归档）；**第二部分**是赛题设计规格（任务定义、观测/动作协议、评分公式、扩展性规则）。

---

# 第一部分 比赛规程

本次比赛采用"统一模板、本地迭代、过程报分、集中提交、统一核验、成果归档"的组织方式。参赛代码与过程测试成绩分别管理，最终评定结果按照赛前公布的评分标准，由组织方统一核定。

## 1. 赛题发布与开发分支

组织方发布官方仓库，提供任务说明、接口定义、示例程序、评测工具、评分标准和提交要求，并为每位参赛者分配唯一编号。

参赛者先确认组织方分配的编号并建立以本人编号命名的开发分支，再在本人 Fork 的 `participant/Pxxx` 目录下创建本人编号目录开展开发；代码推送到本人公开 Fork 的编号分支，组织方可以看到全部开发过程。参赛期间仅可修改本人编号目录，不得修改任务环境、接口定义和评测程序。

## 2. 本地开发与实验记录

参赛者在本地完成策略开发、模型训练、测试及优化，并通过 Git（版本控制工具）保留提交历史和版本标签。

每次重要迭代应记录修改内容、实验配置、测试结果及结论，使最终方案能够追溯至相应代码和模型版本。建议统一维护以下文件：

| 文件                | 内容要求                                   |
| ------------------- | ------------------------------------------ |
| `LOG.md`          | 重要迭代、实验思路、结果分析及后续调整     |
| `experiments.csv` | 实验编号、代码版本、配置、模型及测试成绩   |
| `REPORT.md`       | 最终方案、关键对照实验、复现方法及已知局限 |

统一提交窗口开放前，不得向比赛仓库之外的其他渠道发布比赛实现。

## 3. 过程测试与成绩上报

比赛期间，参赛者可使用官方公开评测程序进行本地测试，并通过指定渠道单独上报测试成绩，无需提交实现代码或模型。

上报内容至少包括参赛编号、方案版本、评测版本、公开测试成绩及更新时间。组织方据此维护过程排行榜，并明确标注"参赛者自行测试，未经组织方核验"。

过程排行榜用于展示进展和提供交流参考，不直接作为最终排名依据。

## 4. 最终提交

组织方在赛前公布统一提交窗口，明确开放时间、截止时间及时区。每位参赛者只能有一个有效的最终提交 PR；PR 标题必须为 `[Pxxx] Final Submission`，并指向本人 Pxxx 分支的最终提交。截止时间前可以关闭旧 PR 并重新提交；截止后以最后一个满足规范且在截止时间前创建的 Final Submission PR 为准。

最终提交所在的编号分支应包含：

* 完整参赛代码及必要配置；
* 与实验记录对应的提交历史；
* `LOG.md`、`experiments.csv`、`REPORT.md`；
* 最终模型、依赖说明及必要的运行文件。

组织方在截止时记录 PR 对应的提交标识和模型摘要，作为核验依据。截止后新增提交或替换模型，不改变已接收的最终版本。

## 5. 组织方统一核验

组织方依据已记录的最终提交获取参赛材料，检查目录合规性、材料完整性、模型可加载性和运行可复现性，并使用统一环境及正式测试集重新评测。

安全要求按三个范围执行：

- 正式推理（组织方容器内）：禁止网络访问、外部进程调用、动态代码执行与不安全对象反序列化，模型在断网、资源受限环境中加载。
- 提交源码（编号目录内全部 Python 文件，含训练代码）：静态审计对不安全反序列化（`pickle`、`dill`、`cloudpickle`、`torch.load` 等）、外部下载与未登记动态载入调用自动拒绝；`subprocess`、`socket`、`urllib`、`requests`、`eval`、`exec`、`ctypes`、`joblib` 等敏感符号命中转人工审核；`artifacts/` 目录下的模型与数据产物仅允许 `npz`、`npy`、`json`、`yaml`、`txt`、`csv` 等受控数据格式。
- 本地训练（参赛者自有设备）：不受上述运行时禁令限制，但随提交入库的代码须满足上一条。

静态审计与人工审核发现违规即拒绝提交。

核验性能分由组织方评测程序生成。参赛者上报的过程成绩、结果文件或自行填写的核验分，不替代组织方的实际评测结果。

无法正常运行、材料缺失或不符合接口要求的提交，按照赛前公布的异常处理规则处置。

## 6. 成绩评定与结果公布

比赛成绩按以下类别分别记录：

| 成绩类别     | 含义                                                                       |
| ------------ | -------------------------------------------------------------------------- |
| 公开测试成绩 | 参赛者在比赛期间自行测试并上报的成绩                                       |
| 核验性能分   | 组织方使用统一环境和正式测试集评测得到的性能分                             |
| 实验质量成绩 | 对实验设计、迭代记录、对照分析及结论质量的评价                             |
| 工程质量成绩 | 对代码组织、依赖管理、运行稳定性及复现说明的评价                           |
| 最终评定     | 录取评审依据：算法榜按核验性能分排序，实验质量与工程质量等级按公布方式使用 |

组织方完成核验与评审后，公布最终评定结果、分项结果和排行榜，并按赛前公布的方式受理成绩复核申请。

## 7. 成果归档

统一提交后，参赛者的完整代码、模型及迭代历史保留在本人公开 Fork 的本人编号目录与对应分支中，最终版本经 Final Submission PR 合入官方仓库归档。组织方统一维护比赛规则、排行榜和成绩档案，形成可查询、可追溯的参赛记录。

---

# 第二部分 赛题设计规格

本文围绕 MPE2（多智能体粒子环境）设计，面向有编程基础、能够使用人工智能辅助开发的大二学生，用于观察其机器人协同策略、强化学习实验与工程交付能力。粒子环境的成绩只能反映相关能力的一部分，不能直接等同于真实机器人比赛能力。

## 8. 设计方向

- 任务采用多机器人覆盖方向，包含有限观测、动态目标和机器人碰撞。
- 官方统一任务规则、输入输出、评测程序；参赛者自由组织策略、网络、训练器和实验。
- 参赛者在本人公开 Fork 的本人编号目录和分支上开发，代码推送到本人公开 Fork 的编号分支，组织方可以看到全部开发过程。
- 比赛期间可单独上报公开测试成绩；最终统一推送，由组织方重新评分。
- 官方仓库统一维护比赛规则、排行榜和成绩档案，各参赛者的完整实现及迭代历史保留在本人编号目录与对应分支中。

已定案并实现的核心约定：逐机器人局部执行、二维连续动作、固定容量观测、每台机器人同时最多覆盖一个目标、持续覆盖计分、固定尺度评分。

设计目标是：在本届声明的能力范围内，参赛者通过调用官方接口完成训练、保存、加载和评分，不必修改官方代码。新增传感器、通信动作或改变动作物理含义属于新规则版本，不能承诺任意未来任务都无需改接口。

## 9. 任务的精确定义

**N 个机器人依据各自的局部观测，在固定时长内持续覆盖 M 个移动目标区域，提高覆盖时间占比，同时降低机器人之间的碰撞。**

| 对象或规则 | 定案                                                                 |
| ---------- | -------------------------------------------------------------------- |
| 空间       | 二维正方形；场地半宽 1.0（见`configs/task-v1.yaml`）               |
| 机器人     | 有半径、速度、驱动力、阻尼和最大速度的粒子；机器人之间有碰撞响应     |
| 目标       | 具有覆盖半径的非碰撞标记，不阻挡机器人；分段转向、有界速度、边界反射 |
| 覆盖       | 机器人中心进入目标覆盖半径；每步重新计算，离开后不继续得分           |
| 容量       | 一台机器人每步最多承担一个目标；同一目标最多计一次覆盖               |
| 感知       | 半径内目标位置和队友状态可见；不模拟遮挡                             |
| 执行       | 每台机器人只能依据自身当前观测及历史记忆输出动作                     |
| 通信       | 首届不增加机器人之间的显式消息通道                                   |
| 回合       | 固定 T 步（公开套件为 10 步）；全覆盖后继续运行，考查持续保持能力    |
| 规模       | 同一回合内 N、M 固定；不同回合可使用不同规模                         |

"覆盖"在本题中指目标区域被机器人服务的数量，并不计算整块场地的扫地面积。任务名称应使用"多目标持续覆盖"，避免让参与者误解成面积扫描。

为避免相邻目标使一台机器人重复得分，官方按当前几何关系建立机器人—目标二分图，在距离满足覆盖条件的边上求最大匹配。匹配数量就是当步覆盖数量；只计匹配数量，不把官方匹配结果作为分工指令传给策略。

有限观测按物理距离筛选，与原生环境"最近若干邻居"的筛选方式不同，由官方观测模块实现。目标速度在执行观测中不直接提供，允许策略从历史位置估计运动趋势；当前目标速度可提供给训练用的全局状态。

### 9.1 一步的执行顺序

1. 从状态 s_t 构造所有机器人的观测。
2. 收集所有机器人的动作；收集完成前不推进环境。
3. 推进一次机器人物理和一次目标运动，执行边界处理，得到 s_(t+1)。
4. 在 s_(t+1) 上计算覆盖和碰撞，生成奖励与下一步观测。
5. 达到 T 步时按时间限制截断；初始状态本身不单独计奖。

机器人边界处理：中心被限制在留出自身半径的场内区域，撞到边界时清零朝外的法向速度，保留切向速度。目标越过其允许范围时反射位置与对应速度分量。边界和大步长情形由单元测试覆盖。

## 10. 系统边界

系统分为四个边界，各自拥有明确的数据权限：

| 边界         | 对外接口                                 | 可访问的信息                       | 修改权限             |
| ------------ | ---------------------------------------- | ---------------------------------- | -------------------- |
| 任务环境     | 创建、重置、推进、查看空间、训练全局状态 | 真实物理状态与目标运动             | 官方                 |
| 参赛训练     | 调用训练环境，自行采样、优化、保存       | 所有训练观测、奖励及当前全局状态   | 参赛者               |
| 正式执行策略 | 加载、回合重置、单机器人决策、关闭       | 本机器人的观测、公开规格、局部历史 | 参赛者实现，官方调度 |
| 评测与归档   | 加载提交、运行测试、算分、输出报告       | 可信环境、私有测试实例、提交身份   | 官方                 |

训练可以使用集中价值网络，即利用全局状态估计整个团队的长期收益；真正执行动作的策略仍只使用局部信息。不能把全队观测字典直接传给同一个正式策略调用，否则局部观测的限制会被合并信息削弱。

正式策略不得通过规定接口以外的渠道访问评测器内部对象、全局状态、其他机器人私有状态或私有测试配置；允许根据规定接口提供的局部观测及本回合历史进行状态估计和预测。违规由静态审计、人工审核与运行核查认定。

策略输出动作后，覆盖、碰撞、奖励和分数都由官方计算。参赛者返回的自定义"分数"不进入核验流程。

## 11. 仓库模块与权限

下表为当前仓库的实际布局：

| 路径                                                               | 职责                                                           | 对参赛者的约束                 |
| ------------------------------------------------------------------ | -------------------------------------------------------------- | ------------------------------ |
| `README.md`                                                      | 仓库导航、目录地图与常用命令                                   | 官方维护                       |
| 本文（赛题与规程）                                                 | 任务规则、协议、评分与提交流程                                 | 官方维护                       |
| `coverage_bench/protocol.py`                                     | 协议规格、上下文类型、策略接口签名                             | 固定公开接口                   |
| `coverage_bench/spaces.py`                                       | 观测展平（`flatten_observation`）等空间辅助                  | 固定公开接口                   |
| `coverage_bench/validation.py`                                   | 类型、形状、掩码、动作范围校验                                 | 固定公开接口                   |
| `coverage_bench/envs/`                                           | 场景、物理、并行环境、观测、奖励、工厂                         | 官方实现                       |
| `coverage_bench/runtime.py`                                      | 策略加载、隔离调用、超时和重置                                 | 官方实现                       |
| `coverage_bench/evaluation.py` / `scoring.py` / `results.py` | 测试集调度、评分聚合、结果文件                                 | 官方实现（results 为公开接口） |
| `configs/task-v1.yaml`                                           | 场地、物理、容量和规则参数                                     | 官方固定                       |
| `configs/public-suite-v1.yaml` / `public-seeds-v1.json`        | 公开测试配置及种子                                             | 官方固定                       |
| `configs/scoring-v1.yaml`                                        | 分组权重、固定尺度及评分版本（正式评分入口按套件推导等值配置） | 官方固定                       |
| `configs/reproduction-tolerance.json`                            | 复核容差                                                       | 官方固定                       |
| `scripts/evaluate_one.py`                                        | 单个提交的评测入口（容器内）                                   | 官方固定                       |
| `scripts/score_all.py`                                           | 批量评分、合并结果                                             | 官方固定，不写第二套公式       |
| `scripts/check_submission.py`                                    | 目录、依赖、模型和接口预检                                     | 官方维护                       |
| `tools/freeze_submission.py`                                     | 生成最终版本登记文件                                           | 官方维护                       |
| `baselines/P901-P903`                                            | 随机、规则、学习三个官方基线（测量工具）                       | 可参考                         |
| `participant/_template/`                                         | 参赛目录模板                                                   | 复制为本人编号目录             |
| `participant/Pxxx/entry.py`                                      | 官方加载该参赛者策略的唯一入口                                 | 参赛者自由实现                 |
| `participant/Pxxx/`                                              | 训练、模型、配置、日志和报告                                   | 参赛者自由组织                 |
| `tests/`                                                         | 官方接口、物理、指标与执行验证                                 | 官方维护                       |

参赛者日常只需要认识环境工厂、协议、本人入口和评分命令。

本地生成的缓存、临时录像和评分报告允许写入忽略目录；这些属于运行产物，不属于修改官方源代码。最终核验从干净的官方版本加载环境，仅导入参赛者编号目录，不能直接运行参赛者分支中的官方评分文件。

## 12. 观测协议

协议版本为 `coverage-policy/1.0`。容量 A=8 台机器人、B=8 个目标已冻结，本届不再扩大。

所有机器人使用相同形状；实际数量通过有效标记表达。实体编号在回合内稳定，目标编号与轨迹参数不建立固定对应关系。N、M 是公开的任务规模；位置、速度及未来轨迹是否可见由字段规则控制。

单机器人观测字段如下（类型定义见 `coverage_bench/protocol.py` 的 `AgentObservation`）：

| 字段               | 类型与形状             | 含义                                             |
| ------------------ | ---------------------- | ------------------------------------------------ |
| `self_state`     | 32 位浮点数，`[5]`   | 自身位置 x、y，速度 vx、vy，机器人半径           |
| `peers`          | 32 位浮点数，`[A,5]` | 按机器人编号排列：相对位置 2、相对速度 2、半径 1 |
| `peer_exists`    | 布尔值，`[A]`        | 对应队友是否属于本回合；自身槽位为假             |
| `peer_visible`   | 布尔值，`[A]`        | 对应队友当前是否处于观测范围                     |
| `targets`        | 32 位浮点数，`[B,3]` | 按目标编号排列：相对位置 2、覆盖半径 1           |
| `target_exists`  | 布尔值，`[B]`        | 对应目标是否属于本回合                           |
| `target_visible` | 布尔值，`[B]`        | 对应目标当前是否处于观测范围                     |
| `agent_index`    | 整数                   | 本机器人在回合内的稳定编号                       |
| `step_index`     | 整数                   | 已推进的步数，从 0 开始                          |
| `time_remaining` | 32 位浮点标量          | 剩余步数除以 T，范围为 0 至 1                    |

规则：

- 距离和位置除以固定场地半宽 L，速度除以固定机器人最大速度 V。相对位置、相对速度不要求落在 -1 至 1 内，不做依赖本局数据的缩放。
- 不存在的实体，其数据行清零。存在但不可见的实体，其数据行也清零，由两个标记区分这两种状态。
- 可见标记为真时，存在标记必须为真。自身在队友表内的行及两个标记都清零。
- 有效实体恰好位于相对原点时，其可见标记仍为真，不能靠"是不是全零"判断可见性。
- 目标离开视野后，官方不继续提供上次位置；历史记忆由参赛者策略维护。
- 不提供目标未来轨迹、环境随机种子、私有测试编号、其他机器人的内部记忆或全局覆盖指标。
- 回合上下文提供 N、M、T、时间步长、感知半径等合法公开参数；这些参数不需要靠读取配置文件猜测。

官方提供 `flatten_observation`（观测展平辅助函数，`coverage_bench/spaces.py`）帮助初学者使用普通网络，展平顺序和版本固定（展平后 104 维）。参赛者也可以自行将实体表组织成序列或图结构，不改动原始协议。

## 13. 动作协议

正式动作统一为 `float32[2]`：x、y 两个方向的归一化驱动命令，每个分量在 -1 至 1 之间。官方物理适配器把命令转换为已声明的驱动力。

- 这是驱动输入，不是下一步位置，也不保证命令为零时立即停止；惯性和阻尼继续生效。
- 对角输入的含义、驱动力系数和速度上限由官方物理配置固定。
- 参赛者可以内部使用离散动作，例如五个方向，再由自己的入口映射成这两个分量。
- 不要求所有参赛者采用同一种算法或网络输出分布。
- 错误形状、非有限数值和超出范围都作为协议错误；不静默裁剪或取模。
- 物理引擎按规则限制机器人最大速度，与修复非法策略输出是两项不同职责。

## 14. 策略生命周期

唯一必须暴露的工厂函数和策略方法如下（类型定义见 `coverage_bench/protocol.py`）：

```python
def build_policy(context: BuildContext) -> Policy:
    ...

class Policy(Protocol):
    def reset(self, context: EpisodeContext) -> None:
        ...

    def act(self, observation: AgentObservation) -> np.ndarray:
        ...

    def close(self) -> None:
        ...
```

| 类型                 | 必须表达的内容                                                          |
| -------------------- | ----------------------------------------------------------------------- |
| `BuildContext`     | 协议规格、已校验的模型目录、运行设备、推理资源限制、独立策略随机源      |
| `EpisodeContext`   | 本机器人编号、N、M、T、允许公开的本局物理与感知参数、独立的策略随机种子 |
| `AgentObservation` | 第 12 节定义的单机器人数据                                              |
| `Policy`           | 加载后的策略实例，允许维护该机器人的回合内记忆                          |

环境种子不放入任何传给策略的上下文。策略随机种子由独立来源生成并记录在评测日志中，不能从它推导测试场景。

正式生命周期为：校验提交 → 创建各机器人策略运行实例 → 加载模型 → 每局重置局部状态 → 多次调用动作函数 → 回合结束 → 清理实例。每局必须清除历史观测、递归网络状态和动作缓存；不同机器人之间不能共享可变记忆，不得跨评测回合适应测试集。

同一提交可以为不同编号加载不同策略，也可以共享相同模型参数；参数在评测过程中冻结。每个实例只访问自己的局部数据。训练时可批量计算多个机器人的动作以提高效率，正式评测仍按单机器人信息权限执行。

官方不统一 `train()`、`update()` 或优化器接口。不同强化学习算法的更新流程差异较大，强行统一这些方法会限制发挥。官方统一的是环境采样与部署推理两端。

## 15. 训练接口与特权信息

训练环境沿用 PettingZoo（多智能体环境接口库）的并行约定：重置返回观测和附加信息；一步接收机器人动作字典，返回观测、奖励、终止、时间截断及附加信息。其全局状态接口适合集中训练、分散执行的方法。[官方并行接口](https://pettingzoo.farama.org/api/parallel/)。

```python
env = make_training_env(config)
observations, infos = env.reset(seed=train_seed)
state = env.state()

next_observations, rewards, terminated, truncated, infos = env.step(actions)
next_state = env.state()
```

训练环境的每个机器人观测和动作，分别与第 12、13 节完全一致。不能出现训练时使用原生扁平观测、正式执行换成另一套字段但没有适配说明的情况。

训练全局状态按固定顺序展平：机器人状态 `[A,5]`、机器人存在标记 `[A]`、目标状态 `[B,5]`、目标存在标记 `[B]`、剩余时间 `[1]`。机器人状态为位置、速度、半径；目标状态为位置、当前速度、覆盖半径。总长度为 `6*A + 6*B + 1`，A=B=8 时为 97。不存在的槽位清零，使用与局部观测一致的归一化基准。

全局状态包含当前所有实体状态，不包含未来运动事件、生成器状态或私有测试配置。该状态仅通过训练接口提供，正式执行策略不得通过规定接口以外的渠道访问它。

训练奖励字典给每个机器人同一个团队奖励。官方累计回报每步只加一次团队奖励，不能把 N 份相同奖励再相加造成规模偏置。附加信息中的奖励分项用于训练和诊断，不转发给正式动作函数。

正常情况下，达到 T 步使用时间截断标记。策略失效和评测基础设施异常由评测器单独记录，不伪装成环境自然终止。训练接口需要保留截断前最后一个观测，以支持算法正确处理时间限制。

参赛者可自行改变训练奖励、增加课程、使用规则预训练或修改自身观测编码。所用方法应记入实验报告；正式评分始终使用官方目标和原始合法观测。

## 16. 模型和提交清单

每个编号目录至少包含以下文件（模板与完整示例见 `participant/_template/`，复制该目录并把目录名与 `participant_id` 改成组织方分配的编号）：

| 文件                        | 内容                                                           |
| --------------------------- | -------------------------------------------------------------- |
| `entry.py`                | 策略工厂入口                                                   |
| `submission.yaml`         | 协议、规则版本、入口、模型清单、推理依赖和复现命令             |
| `artifacts/`              | 本次正式评分实际使用的模型、归一化统计与必要配置               |
| `requirements-infer.lock` | 推理依赖的确定版本；不得替换官方环境与接口包                   |
| `train.py` 或等价入口     | 学习型及含学习组件的混合方法的训练命令；纯规则方法无需训练入口 |
| `LOG.md`                  | 简要实验过程和决策记录                                         |
| `experiments.csv`         | 可比较的实验数据                                               |
| `REPORT.md`               | 最终方案、对照结果、局限与复现说明                             |
| `THIRD_PARTY.md`          | 使用的第三方代码、模型和许可证信息                             |
| `LICENSE`                 | 本人可授权部分的许可证                                         |

提交清单字段建议：

```yaml
participant_id: P0XX
protocol_version: coverage-policy/1.0
task_version: coverage-task/1.0
entrypoint: entry:build_policy
artifact_dir: artifacts
checkpoint_manifest:
  - path: artifacts/policy.npz
    sha256: <模型内容摘要>
inference_lock: requirements-infer.lock
train_command: python train.py --total-steps 1000000
```

清单使用编号目录内的相对路径；拒绝跳出目录的路径和外部可变模型下载。模型内部格式由参赛者决定，官方依赖入口加载。模型必须能在断网的正式运行环境中加载。提交源码中禁止 `pickle.load` / `torch.load` 等不安全反序列化调用（静态审计自动拒绝），模型产物建议使用 `npz` 等数据格式。

最终提交自身的提交哈希写入仓库外的冻结登记记录，不要求写进被该哈希标识的同一个提交，避免自引用。外部模型文件如确有需要，应登记内容摘要并由组织方在截止时取得快照，不能只留下一个随时可替换的下载地址。

## 17. 奖励、指标和评分

设第 t 步的最大覆盖匹配数为 m_t，目标总数为 M。定义覆盖率：

`C_t = m_t / M`。

设某机器人当步与至少一台其他机器人发生重叠接触，则其碰撞标记为 1。定义碰撞参与率：

`K_t = 当步发生至少一次机器人碰撞的机器人数量 / N`。

这种统计不会因同一对机器人被双向遍历而重复计数。另行保存去重后的碰撞对数，用于诊断；不要把这两个量混用。

官方团队奖励：

`r_t = C_t - λ*K_t`，其中 λ = 0.2（`configs/task-v1.yaml` 的 `collision_weight`，已冻结）。

回合累计回报和单位时间回报：

`R = Σ r_t`，`J = R / T`。

每步回报在 `[-λ, 1]` 范围内。奖励在一步物理推进后计算；机器人间碰撞按该时刻中心距小于半径之和判定；目标覆盖按中心距不大于目标覆盖半径判定。

固定长度回合下，优化 R 与 J 等价。跨不同回合长度聚合时使用 J，避免通过更长回合获得更高分。评测输出同时保留原始 R、J、平均覆盖率、平均碰撞参与率、全覆盖时间占比与推理耗时。

训练基准可加入"接近未覆盖目标"的连续奖励帮助学习，但必须明确它是训练辅助项。辅助项不进入官方 J；其系数和移除后的性能差异应可检查。

### 17.1 难度分组

保持相同的接口和核心规则，只改变公开参数范围。公开套件 v1（`configs/public-suite-v1.yaml`）包含基础组与协作组各 2 个场景、每组 2 次策略重复，共 8 个回合：

| 分组               | 配置                            | 观察的能力           |
| ------------------ | ------------------------------- | -------------------- |
| 基础组 basic       | N=M=3，较宽视野、低速目标       | 跑通学习与基本分工   |
| 协作组 cooperation | N=M=3，交叉（crossing）初始布局 | 局部协作、跟踪、避碰 |

容量仍可表示 3 至 8 台的泛化场景；泛化组未纳入 v1 公开套件，留作后续扩展。核验性能分仅由公开套件的基础组与协作组两个 3v3 组构成，各占 50%。公开套件用于开发自测；正式核验采用赛前公布并冻结的任务参数与采样规则，使用未公开且与公开测试不重合的种子。更大规模组合（如 4v5、5v7，均不超过容量上限）不计入核验性能分，可作为拓展实验写进 `REPORT.md`，按实验质量检查项评价，不因规模更大自动加分。

训练配置范围、分组权重和运动机制全部公开。正式测试隐藏具体种子和生成的场景，不临时引入未声明的新规则；精英方案选择和调参只使用训练及公开测试。

### 17.2 分数尺度

评分版本 `coverage-score/1.0`。核验性能分以归一化回合表现为基础：

`S = 1000 * Σ_k w_k * J̄_k`，且 `Σ_k w_k = 1`。

其中 `J = R / T`，`J̄_k` 为第 k 组全部回合 J 的均值，R 为回合回报，T 为回合步数。

本届冻结配置为两组各权重 0.5，固定尺度 b_k = 0.0、a_k = 1.0。公开套件回合长度 T = 10 时，该式与"两组平均回合回报加权后 × 100"数值一致。固定尺度不随参赛者成绩变化；三档基线成绩为参考表现，用于检查评测流程与任务区分度，不作评分锚点。若后续版本改用实测校准锚点，须在开赛前公布校准套件、校准数据与新锚点。固定尺度与组权重属于评分版本内容。

三基线正式运行成绩（2026-09-19，逐案明细与判读见 `docs/baselines-anchor-draft.md`）：

| 基线 | 定位                  | performance_score |
| ---- | --------------------- | ----------------- |
| P901 | random（随机）        | 58.33             |
| P902 | rule（规则）          | 175.00            |
| P903 | learning（1M 步 PPO） | 116.67            |

random 与 rule 拉开约 3 倍差距，任务区分度的下限成立。

核验性能分的理论范围为 -200 至 1000（固定尺度与碰撞权重 0.2 下）：单步回报下限为 -λ，J 可为负；零分或负分是有效成绩，与提交无效区分。

所有参赛者使用相同的场景种子和策略重复采样安排；保存逐回合原始记录。报告每组均值和按场景聚合的不确定性区间。排名过近时按照赛前公布的统一补测规则处理，不能事后挑选有利种子；补测由组织方统一执行，不自动触发。

### 17.3 选拔成绩

先设置可复现、接口合格、交付完整的资格门槛，再发布三列：核验性能分、实验质量等级、工程质量等级。算法榜按核验性能分排序；实验质量等级与工程质量等级不改变算法榜排序，作为录取评审的独立依据，具体使用方式（资格门槛、同分优先或独立评审）在开赛规则附件中公布并带版本号，不写进策略协议。

实验与工程各设 0 至 5 个明确检查项，避免按提交次数、代码行数或日志长度加分。实验检查为对照设计、变量控制、重复实验、结论证据、失败分析；工程检查为可运行入口、依赖锁定、模块组织、错误处理、复现说明。若采用加权总分，必须赛前公布权重和具体判定标准，并在结果中保留各分项。

## 18. 评测运行与结果文件

参赛者本地提交预检：

```bash
python scripts/check_submission.py --submission participant/P0XX --output <输出目录>
```

组织方批量评分（每份提交在独立容器内评测，逐份隔离失败）：

```bash
python scripts/score_all.py \
  --submissions <提交根目录> \
  --suite configs/public-suite-v1.yaml \
  --seeds configs/public-seeds-v1.json \
  --image coverage-bench:verify-v1 \
  --output <输出目录>
```

批量脚本只枚举、调用和汇总；评分公式只有一处实现。单个提交失败不得中断其他人的评分。

正式评测顺序：解析冻结提交 → 校验协议与模型摘要 → 静态审计 → 准备独立推理依赖 → 容器内加载策略 → 执行协议检查 → 运行全部测试 → 从原始指标算分 → 写出结构化结果。

可信评测器持有环境与测试集；单次评测内，策略与评测环境运行于同一受控进程，策略仅收到合法上下文和局部观测。评测在组织方容器内逐份隔离执行；容器内禁止网络访问和跨机器人共享可变文件，策略只读必要的策略代码及模型、只写自己的临时目录。

时间限制分为初始化、单步决策、单回合及整个任务四层（`coverage_bench/protocol.py` 的 `ResourceLimits`）；内存和模型体积限制独立配置。推理统一在中央处理器上进行。训练耗时作为实验信息记录，不作为不同个人硬件之间的直接扣分依据。

协议错误、推理超时、加载失败必须输出明确状态。正式有效分要求完整跑完规定测试；不能删去失败回合后对剩余成绩求平均。参赛者实现问题标记为提交无效，保留诊断结果但不计正式分；基础设施故障最多重试 1 次，重试与补测由组织方执行并记录。

结果文件（`result.json`，结构见 `coverage_bench/results.py`）至少包含：结果格式版本、参赛编号、状态、协议/任务/评分三个独立版本、套件标识与摘要、运行条件、分组指标、`performance_score`、逐回合记录位置与错误类别。配套输出 `episodes.csv`（逐回合数据）与 `timings.npz`（耗时分布），均带内容摘要。

完整核验记录由组织方保存；对外过程报分只输出必要摘要。私有测试种子不随中途诊断传给参赛策略，赛后是否公开全部测试按赛前规则执行。

## 19. 实验记录、编号和公开授权

`LOG.md` 每次重要实验只需要说明：问题或假设、改变了什么、如何测试、结果和下一步。`experiments.csv` 建议记录实验编号、代码标识、训练配置摘要、训练种子、训练步数、模型摘要、公开测试版本、各组指标和备注。代码提交记录不能独自证明实验发生时间，也不能代替实验数据。

`REPORT.md` 说明最终策略、训练流程、关键对照、失败场景、资源使用和复现命令。原始训练日志可以保存为模型产物，不要求把全部中间模型塞进官方仓库。

编号公开、姓名映射由组织方私下保存。但比赛仓库仍会显示账号、提交作者与邮箱，编号只实现成绩展示的去实名，不能承诺彻底匿名。开赛时提供作者信息与敏感文件检查说明，避免赛后重写历史破坏冻结提交。

原创代码采用 MIT（宽松开源许可证）可以作为事先明确的参赛公开约定。提交时应明确确认本人可授权代码的公开许可，第三方代码保留原许可证和署名。将沉默直接视为完整授权不适合作为项目的默认操作规则。代码公开后第三方可能已经保存副本，移除官方索引不能保证所有副本消失。

评测漏洞的上报与认定规则见 `docs/vulnerability-disclosure.md`。

## 20. 扩展性验收

| 参赛者希望做的改动                   | 是否修改官方接口   | 所需支持                     |
| ------------------------------------ | ------------------ | ---------------------------- |
| 更换强化学习算法或优化器             | 否                 | 自由训练器，统一环境采样     |
| 更换普通网络、注意力网络或图网络     | 否                 | 实体表、稳定编号、有效标记   |
| 增加递归记忆或历史窗口               | 否                 | 每机器人实例、明确回合重置   |
| 使用集中价值网络                     | 否                 | 训练专用全局状态             |
| 参数共享或不同角色策略               | 否                 | 本机器人编号及独立加载状态   |
| 规则与学习组合                       | 否                 | 只需满足局部信息和动作限制   |
| 改训练奖励、课程或数据采样           | 否                 | 自己的训练包装与记录         |
| 从 3 台扩展到已声明容量以内          | 否                 | 固定容量和实际存在标记       |
| 更换已声明范围内的目标轨迹和视野     | 否                 | 官方场景配置与统一字段语义   |
| 增加摄像头图像、通信动作或新动作维度 | 是，进入新主版本   | 属于规则和信息权限变化       |
| 超过首届容量、引入回合内机器人失效   | 需要重新定义与验证 | 不能只加一个配置键就声称兼容 |

不使用任意字典字段向正式策略临时传隐藏信息。新增可选能力需要明确字段、权限、默认行为和版本，不能以"可扩展"为由留下不受约束的数据通道。

版本需要分别管理：

| 版本       | 变更范围                               | 兼容约束                                                                   |
| ---------- | -------------------------------------- | -------------------------------------------------------------------------- |
| 协议版本   | 字段、形状、归一化、动作含义、生命周期 | 删除字段或改变语义须升级主版本；未知可选元数据只能出现在明确允许的元数据区 |
| 任务版本   | 物理、目标运动、可见性和覆盖规则       | 输入形状相同也可能导致旧模型失效，不能只检查协议版本                       |
| 评分版本   | 指标聚合、分组权重和归一化尺度         | 结果必须携带此版本，不同版本的分数不得直接混排                             |
| 测试集版本 | 场景实例及种子安排                     | 相同任务下不同套件分别标识，公开与私有结果分列                             |

开赛后若修复影响行为的错误，官方统一公告，提供固定的新版本并安排所有受影响提交重测；不通过悄悄修改默认参数改变排名。

## 21. 官方基线与验收状态

官方发布材料包含：

1. 随机策略基线（`baselines/P901`）：证明安装、加载、重置、动作和输出路径可用。
2. 合法局部规则策略基线（`baselines/P902`）：用相同观测条件检验任务是否容易被简单追踪或分工方法做满。
3. 学习基准（`baselines/P903`）：提供训练配置、种子、模型、公开测试成绩和可复现命令（SB3 PPO，1M 步 CPU 训练约 3 分钟）；证明学生能够在公布的资源预算内完成一个有效学习循环。

三基线正式成绩见第 17.2 节与 `docs/baselines-anchor-draft.md`。

主要验收状态（验收需求清单 `tests/acceptance/requirements-v1.json`，共 69 条，随附运行手册 `docs/acceptance/`）：

- 同一固定版本与种子可重建场景；渲染开关不改变物理结果。
- 所有正式规模、视野和目标轨迹配置都产生符合规格的观测。
- 改动当前视野外的实体状态时，合法当前观测不发生不应有的变化。
- 不可见状态通过全局状态、附加信息、配置文件或共享内存泄漏的路径受到控制。
- 相邻目标、多个机器人争抢目标、两台机器人碰撞等小场景的指标有手工可核对的答案。
- 相同团队奖励不会按机器人数量重复累计。
- 回合重置清空记忆；调换回合顺序不应因残留状态改变结果。
- 单步、回合和任务超时能被终止并形成结构化错误，不阻塞整批评测。
- 非法动作和失败回合不会被悄悄修复、删除或少计。
- 更换网络、使用带记忆的策略、改变合法规模不需要修改官方协议。
- 本地公开评测与官方对同一公开套件的复核在公布容差内一致。
- 模型、依赖、评分版本或提交变更可以从结果中被识别。
- 简单规则、学习基准与经过改进的策略具有可测的性能差异。

## 22. 实施状态与冻结清单

| 阶段          | 交付物                                     | 状态                                   |
| ------------- | ------------------------------------------ | -------------------------------------- |
| A：规则与协议 | 任务文本、字段表、方法签名、版本规则       | 完成                                   |
| B：环境与指标 | 动态目标场景、观测、物理适配、奖励         | 完成                                   |
| C：加载与评测 | 工作进程、单人评分、结果文件、预检         | 完成                                   |
| D：基准与校准 | 随机、规则和学习基准、耗时与分差报告       | 完成（2026-09-19 正式运行）            |
| E：开赛冻结   | 固定版本、公开套件、封存私有套件、提交规则 | 部分完成（私有套件封存与正式发布待办） |

已冻结的数值以 `configs/` 为准：容量 A=B=8、场地半宽、时间步长、回合时长、半径、驱动力、阻尼、最大速度、边界行为、目标运动分布、感知范围、碰撞权重 λ=0.2、难度权重、评分固定尺度、评测局数、策略重复次数、推理资源限制、有效误差容差（`configs/reproduction-tolerance.json`）。提交时间与时区待赛前公布；补测规则与重试上限在开赛规则中公布并冻结

# MPE2 协同覆盖选拔赛：赛题与规程

版本：v1.1（2026-09-22 更新，取代 2026-09-20 整合的 v1.0 版本；v1.0 另行取代原《比赛程序概览.md》与《MPE2-协同覆盖选拔赛-设计规格-v0.1.md》）。

状态：任务环境、评测程序与评分管道已实现并通过验收（验收需求清单 `tests/acceptance/requirements-v1.json`，共 69 条）；三档官方基线已于 2026-09-19 完成正式运行，成绩锚点记录见 `docs/baselines-anchor-draft.md`。文中数值凡已写入 `configs/` 的均属冻结值，配置文件是权威来源。

本文分两部分：**第一部分**是比赛组织规程（编号目录、报分、提交、核验、归档）；**第二部分**是赛题设计规格（任务定义、观测/动作协议、评分公式、扩展性规则）。

---

# 第一部分 比赛规程

本次比赛采用"统一模板、本地迭代、过程报分、集中提交、统一核验、成果归档"的组织方式。参赛代码与过程测试成绩分别管理，最终成绩按照赛前公布的评分标准，由组织方统一核定。

## 1. 赛题发布与开发分支

组织方发布官方仓库，提供任务说明、接口定义、示例程序、评测工具、评分标准和提交要求，并为每位参赛者分配唯一编号。

参赛者在本人 Fork 的 `participant/Pxxx` 目录下创建本人编号目录，并建立以本人编号命名的开发分支开展开发；代码推送到本人公开 Fork 的编号分支，组织方可以看到全部开发过程。参赛期间仅可修改本人编号目录，不得修改任务环境、接口定义和评测程序。

## 2. 本地开发与实验记录

参赛者在本地完成策略开发、模型训练、测试及优化，并通过 Git（版本控制工具）保留提交历史和版本标签。

每次重要迭代应记录修改内容、实验配置、测试结果及结论，使最终方案能够追溯至相应代码和模型版本。建议统一维护以下文件：

| 文件                | 内容要求                                   |
| ------------------- | ------------------------------------------ |
| `LOG.md`          | 重要迭代、实验思路、结果分析及后续调整     |
| `experiments.csv` | 实验编号、代码版本、配置、模型及测试成绩   |
| `REPORT.md`       | 最终方案、关键对照实验、复现方法及已知局限 |

统一提交窗口开放前，不得向比赛仓库之外的其他渠道发布比赛实现。

## 3. 过程测试与成绩上报

比赛期间，参赛者可使用官方公开评测程序进行本地测试，并通过指定渠道单独上报测试成绩，无需提交实现代码或模型。

上报内容至少包括参赛编号、方案版本、评测版本、公开测试成绩及更新时间。组织方据此维护过程排行榜，并明确标注"参赛者自行测试，未经组织方核验"。

过程排行榜用于展示进展和提供交流参考，不直接作为最终排名依据。

## 4. 最终提交

组织方在赛前公布统一提交窗口，明确开放时间、截止时间及时区。每位参赛者只能有一个有效的最终提交 PR；PR 标题必须为 `[Pxxx] Final Submission`，并指向本人 Pxxx 分支的最终提交。截止时间前可以关闭旧 PR 并重新提交；截止后以最后一个满足规范且在截止时间前创建的 Final Submission PR 为准。

最终提交所在的编号分支应包含：

* 完整参赛代码及必要配置；
* 与实验记录对应的提交历史；
* `LOG.md`、`experiments.csv`、`REPORT.md`；
* 最终模型、依赖说明及必要的运行文件。

组织方在截止时记录 PR 对应的提交标识和模型摘要，作为核验依据。截止后新增提交或替换模型，不改变已接收的最终版本。

## 5. 组织方统一核验

组织方依据已记录的最终提交获取参赛材料，检查目录合规性、材料完整性、模型可加载性和运行可复现性，并使用统一环境及正式测试集重新评测。

核验成绩由组织方评测程序生成。参赛者上报的过程成绩、结果文件或自行填写的核验分，不替代组织方的实际评测结果。

无法正常运行、材料缺失或不符合接口要求的提交，按照赛前公布的异常处理规则处置。

## 6. 成绩评定与结果公布

比赛成绩按以下类别分别记录：

| 成绩类别     | 含义                                             |
| ------------ | ------------------------------------------------ |
| 公开测试成绩 | 参赛者在比赛期间自行测试并上报的成绩             |
| 核验成绩     | 组织方使用统一环境和正式测试集得到的成绩         |
| 实验质量成绩 | 对实验设计、迭代记录、对照分析及结论质量的评价   |
| 工程质量成绩 | 对代码组织、依赖管理、运行稳定性及复现说明的评价 |
| 最终成绩     | 按赛前公布的评分标准汇总形成的成绩               |

组织方完成核验与评审后，公布最终成绩、分项结果和排行榜，并按赛前公布的方式受理成绩复核申请。

## 7. 成果归档

统一提交后，参赛者的完整代码、模型及迭代历史保留在本人公开 Fork 的本人编号目录与对应分支中，最终版本经 Final Submission PR 合入官方仓库归档。组织方统一维护比赛规则、排行榜和成绩档案，形成可查询、可追溯的参赛记录。

---

# 第二部分 赛题设计规格

本文围绕 MPE2（多智能体粒子环境）设计，面向有编程基础、能够使用人工智能辅助开发的大二学生，用于观察其机器人协同策略、强化学习实验与工程交付能力。粒子环境的成绩只能反映相关能力的一部分，不能直接等同于真实机器人比赛能力。

## 8. 设计方向

- 任务采用多机器人覆盖方向，包含有限观测、动态目标和机器人碰撞。
- 官方统一任务规则、输入输出、评测程序；参赛者自由组织策略、网络、训练器和实验。
- 参赛者在本人公开 Fork 的本人编号目录和分支上开发，代码推送到本人公开 Fork 的编号分支，组织方可以看到全部开发过程。
- 比赛期间可单独上报公开测试成绩；最终统一推送，由组织方重新评分。
- 官方仓库统一维护比赛规则、排行榜和成绩档案，各参赛者的完整实现及迭代历史保留在本人编号目录与对应分支中。

已定案并实现的核心约定：逐机器人局部执行、二维连续动作、固定容量观测、每台机器人同时最多覆盖一个目标、持续覆盖计分、基准相对分数。

设计目标是：在本届声明的能力范围内，参赛者通过调用官方接口完成训练、保存、加载和评分，不必修改官方代码。新增传感器、通信动作或改变动作物理含义属于新规则版本，不能承诺任意未来任务都无需改接口。

## 9. 任务的精确定义

**N 个机器人依据各自的局部观测，在固定时长内持续覆盖 M 个移动目标区域，提高覆盖时间占比，同时降低机器人之间的碰撞。**

| 对象或规则 | 定案                                                                 |
| ---------- | -------------------------------------------------------------------- |
| 空间       | 二维正方形；场地半宽 1.0（见`configs/task-v1.yaml`）               |
| 机器人     | 有半径、速度、驱动力、阻尼和最大速度的粒子；机器人之间有碰撞响应     |
| 目标       | 具有覆盖半径的非碰撞标记，不阻挡机器人；分段转向、有界速度、边界反射 |
| 覆盖       | 机器人中心进入目标覆盖半径；每步重新计算，离开后不继续得分           |
| 容量       | 一台机器人每步最多承担一个目标；同一目标最多计一次覆盖               |
| 感知       | 半径内目标位置和队友状态可见；不模拟遮挡                             |
| 执行       | 每台机器人只能依据自身当前观测及历史记忆输出动作                     |
| 通信       | 首届不增加机器人之间的显式消息通道                                   |
| 回合       | 固定 T 步（公开套件为 10 步）；全覆盖后继续运行，考查持续保持能力    |
| 规模       | 同一回合内 N、M 固定；不同回合可使用不同规模                         |

"覆盖"在本题中指目标区域被机器人服务的数量，并不计算整块场地的扫地面积。任务名称应使用"多目标持续覆盖"，避免让参与者误解成面积扫描。

为避免相邻目标使一台机器人重复得分，官方按当前几何关系建立机器人—目标二分图，在距离满足覆盖条件的边上求最大匹配。匹配数量就是当步覆盖数量；只计匹配数量，不把官方匹配结果作为分工指令传给策略。

有限观测按物理距离筛选，与原生环境"最近若干邻居"的筛选方式不同，由官方观测模块实现。目标速度在执行观测中不直接提供，允许策略从历史位置估计运动趋势；当前目标速度可提供给训练用的全局状态。

### 9.1 一步的执行顺序

1. 从状态 s_t 构造所有机器人的观测。
2. 收集所有机器人的动作；收集完成前不推进环境。
3. 推进一次机器人物理和一次目标运动，执行边界处理，得到 s_(t+1)。
4. 在 s_(t+1) 上计算覆盖和碰撞，生成奖励与下一步观测。
5. 达到 T 步时按时间限制截断；初始状态本身不单独计奖。

机器人边界处理：中心被限制在留出自身半径的场内区域，撞到边界时清零朝外的法向速度，保留切向速度。目标越过其允许范围时反射位置与对应速度分量。边界和大步长情形由单元测试覆盖。

## 10. 系统边界

系统分为四个边界，各自拥有明确的数据权限：

| 边界         | 对外接口                                 | 可访问的信息                       | 修改权限             |
| ------------ | ---------------------------------------- | ---------------------------------- | -------------------- |
| 任务环境     | 创建、重置、推进、查看空间、训练全局状态 | 真实物理状态与目标运动             | 官方                 |
| 参赛训练     | 调用训练环境，自行采样、优化、保存       | 所有训练观测、奖励及当前全局状态   | 参赛者               |
| 正式执行策略 | 加载、回合重置、单机器人决策、关闭       | 本机器人的观测、公开规格、局部历史 | 参赛者实现，官方调度 |
| 评测与归档   | 加载提交、运行测试、算分、输出报告       | 可信环境、私有测试实例、提交身份   | 官方                 |

训练可以使用集中价值网络，即利用全局状态估计整个团队的长期收益；真正执行动作的策略仍只使用局部信息。不能把全队观测字典直接传给同一个正式策略调用，否则局部观测的限制会被合并信息削弱。

策略输出动作后，覆盖、碰撞、奖励和分数都由官方计算。参赛者返回的自定义"分数"不进入核验流程。

## 11. 仓库模块与权限

下表为当前仓库的实际布局：

| 路径                                                               | 职责                                          | 对参赛者的约束                 |
| ------------------------------------------------------------------ | --------------------------------------------- | ------------------------------ |
| `README.md`                                                      | 仓库导航、目录地图与常用命令                  | 官方维护                       |
| 本文（赛题与规程）                                                 | 任务规则、协议、评分与提交流程                | 官方维护                       |
| `coverage_bench/protocol.py`                                     | 协议规格、上下文类型、策略接口签名            | 固定公开接口                   |
| `coverage_bench/spaces.py`                                       | 观测展平（`flatten_observation`）等空间辅助 | 固定公开接口                   |
| `coverage_bench/validation.py`                                   | 类型、形状、掩码、动作范围校验                | 固定公开接口                   |
| `coverage_bench/envs/`                                           | 场景、物理、并行环境、观测、奖励、工厂        | 官方实现                       |
| `coverage_bench/runtime.py`                                      | 策略加载、隔离调用、超时和重置                | 官方实现                       |
| `coverage_bench/evaluation.py` / `scoring.py` / `results.py` | 测试集调度、评分聚合、结果文件                | 官方实现（results 为公开接口） |
| `configs/task-v1.yaml`                                           | 场地、物理、容量和规则参数                    | 官方固定                       |
| `configs/public-suite-v1.yaml` / `public-seeds-v1.json`        | 公开测试配置及种子                            | 官方固定                       |
| `configs/scoring-v1.yaml`                                        | 分组权重、基准锚点及评分版本                  | 官方固定                       |
| `configs/reproduction-tolerance.json`                            | 复核容差                                      | 官方固定                       |
| `scripts/evaluate_one.py`                                        | 单个提交的评测入口（容器内）                  | 官方固定                       |
| `scripts/score_all.py`                                           | 批量评分、合并结果                            | 官方固定，不写第二套公式       |
| `scripts/check_submission.py`                                    | 目录、依赖、模型和接口预检                    | 官方维护                       |
| `tools/freeze_submission.py`                                     | 生成最终版本登记文件                          | 官方维护                       |
| `baselines/P901-P903`                                            | 随机、规则、学习三个官方基线（测量工具）      | 可参考                         |
| `participant/_template/`                                         | 参赛目录模板                                  | 复制为本人编号目录             |
| `participant/Pxxx/entry.py`                                      | 官方加载该参赛者策略的唯一入口                | 参赛者自由实现                 |
| `participant/Pxxx/`                                              | 训练、模型、配置、日志和报告                  | 参赛者自由组织                 |
| `tests/`                                                         | 官方接口、物理、指标与执行验证                | 官方维护                       |

参赛者日常只需要认识环境工厂、协议、本人入口和评分命令。

本地生成的缓存、临时录像和评分报告允许写入忽略目录；这些属于运行产物，不属于修改官方源代码。最终核验从干净的官方版本加载环境，仅导入参赛者编号目录，不能直接运行参赛者分支中的官方评分文件。

## 12. 观测协议

协议版本为 `coverage-policy/1.0`。容量 A=8 台机器人、B=8 个目标已冻结，本届不再扩大。

所有机器人使用相同形状；实际数量通过有效标记表达。实体编号在回合内稳定，目标编号与轨迹参数不建立固定对应关系。N、M 是公开的任务规模；位置、速度及未来轨迹是否可见由字段规则控制。

单机器人观测字段如下（类型定义见 `coverage_bench/protocol.py` 的 `AgentObservation`）：

| 字段               | 类型与形状             | 含义                                             |
| ------------------ | ---------------------- | ------------------------------------------------ |
| `self_state`     | 32 位浮点数，`[5]`   | 自身位置 x、y，速度 vx、vy，机器人半径           |
| `peers`          | 32 位浮点数，`[A,5]` | 按机器人编号排列：相对位置 2、相对速度 2、半径 1 |
| `peer_exists`    | 布尔值，`[A]`        | 对应队友是否属于本回合；自身槽位为假             |
| `peer_visible`   | 布尔值，`[A]`        | 对应队友当前是否处于观测范围                     |
| `targets`        | 32 位浮点数，`[B,3]` | 按目标编号排列：相对位置 2、覆盖半径 1           |
| `target_exists`  | 布尔值，`[B]`        | 对应目标是否属于本回合                           |
| `target_visible` | 布尔值，`[B]`        | 对应目标当前是否处于观测范围                     |
| `agent_index`    | 整数                   | 本机器人在回合内的稳定编号                       |
| `step_index`     | 整数                   | 已推进的步数，从 0 开始                          |
| `time_remaining` | 32 位浮点标量          | 剩余步数除以 T，范围为 0 至 1                    |

规则：

- 距离和位置除以固定场地半宽 L，速度除以固定机器人最大速度 V。相对位置、相对速度不要求落在 -1 至 1 内，不做依赖本局数据的缩放。
- 不存在的实体，其数据行清零。存在但不可见的实体，其数据行也清零，由两个标记区分这两种状态。
- 可见标记为真时，存在标记必须为真。自身在队友表内的行及两个标记都清零。
- 有效实体恰好位于相对原点时，其可见标记仍为真，不能靠"是不是全零"判断可见性。
- 目标离开视野后，官方不继续提供上次位置；历史记忆由参赛者策略维护。
- 不提供目标未来轨迹、环境随机种子、私有测试编号、其他机器人的内部记忆或全局覆盖指标。
- 回合上下文提供 N、M、T、时间步长、感知半径等合法公开参数；这些参数不需要靠读取配置文件猜测。

官方提供 `flatten_observation`（观测展平辅助函数，`coverage_bench/spaces.py`）帮助初学者使用普通网络，展平顺序和版本固定（展平后 104 维）。参赛者也可以自行将实体表组织成序列或图结构，不改动原始协议。

## 13. 动作协议

正式动作统一为 `float32[2]`：x、y 两个方向的归一化驱动命令，每个分量在 -1 至 1 之间。官方物理适配器把命令转换为已声明的驱动力。

- 这是驱动输入，不是下一步位置，也不保证命令为零时立即停止；惯性和阻尼继续生效。
- 对角输入的含义、驱动力系数和速度上限由官方物理配置固定。
- 参赛者可以内部使用离散动作，例如五个方向，再由自己的入口映射成这两个分量。
- 不要求所有参赛者采用同一种算法或网络输出分布。
- 错误形状、非有限数值和超出范围都作为协议错误；不静默裁剪或取模。
- 物理引擎按规则限制机器人最大速度，与修复非法策略输出是两项不同职责。

## 14. 策略生命周期

唯一必须暴露的工厂函数和策略方法如下（类型定义见 `coverage_bench/protocol.py`）：

```python
def build_policy(context: BuildContext) -> Policy:
    ...

class Policy(Protocol):
    def reset(self, context: EpisodeContext) -> None:
        ...

    def act(self, observation: AgentObservation) -> np.ndarray:
        ...

    def close(self) -> None:
        ...
```

| 类型                 | 必须表达的内容                                                          |
| -------------------- | ----------------------------------------------------------------------- |
| `BuildContext`     | 协议规格、已校验的模型目录、运行设备、推理资源限制、独立策略随机源      |
| `EpisodeContext`   | 本机器人编号、N、M、T、允许公开的本局物理与感知参数、独立的策略随机种子 |
| `AgentObservation` | 第 12 节定义的单机器人数据                                              |
| `Policy`           | 加载后的策略实例，允许维护该机器人的回合内记忆                          |

环境种子不放入任何传给策略的上下文。策略随机种子由独立来源生成并记录在评测日志中，不能从它推导测试场景。

正式生命周期为：校验提交 → 创建各机器人策略运行实例 → 加载模型 → 每局重置局部状态 → 多次调用动作函数 → 回合结束 → 清理实例。每局必须清除历史观测、递归网络状态和动作缓存；不同机器人之间不能共享可变记忆，不得跨评测回合适应测试集。

同一提交可以为不同编号加载不同策略，也可以共享相同模型参数；参数在评测过程中冻结。每个实例只访问自己的局部数据。训练时可批量计算多个机器人的动作以提高效率，正式评测仍按单机器人信息权限执行。

官方不统一 `train()`、`update()` 或优化器接口。不同强化学习算法的更新流程差异较大，强行统一这些方法会限制发挥。官方统一的是环境采样与部署推理两端。

## 15. 训练接口与特权信息

训练环境沿用 PettingZoo（多智能体环境接口库）的并行约定：重置返回观测和附加信息；一步接收机器人动作字典，返回观测、奖励、终止、时间截断及附加信息。其全局状态接口适合集中训练、分散执行的方法。[官方并行接口](https://pettingzoo.farama.org/api/parallel/)。

```python
env = make_training_env(config)
observations, infos = env.reset(seed=train_seed)
state = env.state()

next_observations, rewards, terminated, truncated, infos = env.step(actions)
next_state = env.state()
```

训练环境的每个机器人观测和动作，分别与第 12、13 节完全一致。不能出现训练时使用原生扁平观测、正式执行换成另一套字段但没有适配说明的情况。

训练全局状态按固定顺序展平：机器人状态 `[A,5]`、机器人存在标记 `[A]`、目标状态 `[B,5]`、目标存在标记 `[B]`、剩余时间 `[1]`。机器人状态为位置、速度、半径；目标状态为位置、当前速度、覆盖半径。总长度为 `6*A + 6*B + 1`，A=B=8 时为 97。不存在的槽位清零，使用与局部观测一致的归一化基准。

全局状态包含当前所有实体状态，不包含未来运动事件、生成器状态或私有测试配置。该状态仅服务训练，正式策略工作进程接触不到它。

训练奖励字典给每个机器人同一个团队奖励。官方累计回报每步只加一次团队奖励，不能把 N 份相同奖励再相加造成规模偏置。附加信息中的奖励分项用于训练和诊断，不转发给正式动作函数。

正常情况下，达到 T 步使用时间截断标记。策略失效和评测基础设施异常由评测器单独记录，不伪装成环境自然终止。训练接口需要保留截断前最后一个观测，以支持算法正确处理时间限制。

参赛者可自行改变训练奖励、增加课程、使用规则预训练或修改自身观测编码。所用方法应记入实验报告；正式评分始终使用官方目标和原始合法观测。

## 16. 模型和提交清单

每个编号目录至少包含以下文件（模板与完整示例见 `participant/_template/`，复制该目录并把目录名与 `participant_id` 改成组织方分配的编号）：

| 文件                        | 内容                                               |
| --------------------------- | -------------------------------------------------- |
| `entry.py`                | 策略工厂入口                                       |
| `submission.yaml`         | 协议、规则版本、入口、模型清单、推理依赖和复现命令 |
| `artifacts/`              | 本次正式评分实际使用的模型、归一化统计与必要配置   |
| `requirements-infer.lock` | 推理依赖的确定版本；不得替换官方环境与接口包       |
| `train.py` 或等价入口     | 学习模型的训练命令                                 |
| `LOG.md`                  | 简要实验过程和决策记录                             |
| `experiments.csv`         | 可比较的实验数据                                   |
| `REPORT.md`               | 最终方案、对照结果、局限与复现说明                 |
| `THIRD_PARTY.md`          | 使用的第三方代码、模型和许可证信息                 |
| `LICENSE`                 | 本人可授权部分的许可证                             |

提交清单字段建议：

```yaml
participant_id: P0XX
protocol_version: coverage-policy/1.0
task_version: coverage-task/1.0
entrypoint: entry:build_policy
artifact_dir: artifacts
checkpoint_manifest:
  - path: artifacts/policy.npz
    sha256: <模型内容摘要>
inference_lock: requirements-infer.lock
train_command: python train.py --total-steps 1000000
```

清单使用编号目录内的相对路径；拒绝跳出目录的路径和外部可变模型下载。模型内部格式由参赛者决定，官方依赖入口加载。模型必须能在断网的正式运行环境中加载。提交源码中禁止 `pickle.load` / `torch.load` 等不安全反序列化调用（静态审计硬性拒绝），模型产物建议使用 `npz` 等数据格式。

最终提交自身的提交哈希写入仓库外的冻结登记记录，不要求写进被该哈希标识的同一个提交，避免自引用。外部模型文件如确有需要，应登记内容摘要并由组织方在截止时取得快照，不能只留下一个随时可替换的下载地址。

## 17. 奖励、指标和评分

设第 t 步的最大覆盖匹配数为 m_t，目标总数为 M。定义覆盖率：

`C_t = m_t / M`。

设某机器人当步与至少一台其他机器人发生重叠接触，则其碰撞标记为 1。定义碰撞参与率：

`K_t = 当步发生至少一次机器人碰撞的机器人数量 / N`。

这种统计不会因同一对机器人被双向遍历而重复计数。另行保存去重后的碰撞对数，用于诊断；不要把这两个量混用。

官方团队奖励：

`r_t = C_t - λ*K_t`，其中 λ = 0.2（`configs/task-v1.yaml` 的 `collision_weight`，已冻结）。

回合累计回报和单位时间回报：

`R = Σ r_t`，`J = R / T`。

每步回报在 `[-λ, 1]` 范围内。奖励在一步物理推进后计算；机器人间碰撞按该时刻中心距小于半径之和判定；目标覆盖按中心距不大于目标覆盖半径判定。

固定长度回合下，优化 R 与 J 等价。跨不同回合长度聚合时使用 J，避免通过更长回合获得更高分。评测输出同时保留原始 R、J、平均覆盖率、平均碰撞参与率、全覆盖时间占比与推理耗时。

训练基准可加入"接近未覆盖目标"的连续奖励帮助学习，但必须明确它是训练辅助项。辅助项不进入官方 J；其系数和移除后的性能差异应可检查。

### 17.1 难度分组

保持相同的接口和核心规则，只改变公开参数范围。公开套件 v1（`configs/public-suite-v1.yaml`）包含基础组与协作组各 2 个场景、每组 2 次策略重复，共 8 个回合：

| 分组               | 配置                            | 观察的能力           |
| ------------------ | ------------------------------- | -------------------- |
| 基础组 basic       | N=M=3，较宽视野、低速目标       | 跑通学习与基本分工   |
| 协作组 cooperation | N=M=3，更具挑战的运动与视野参数 | 局部协作、跟踪、避碰 |

容量仍可表示 3 至 8 台的泛化场景；泛化组未纳入 v1 公开套件，留作后续扩展。本届正式计分范围仅限公开套件的 3v3 两组（基础组、协作组）；更大规模组合（如 4v5、5v7，均不超过容量上限）不计入正式成绩，可作为拓展实验写进 `REPORT.md`，由实验质量等级体现。

训练配置范围、分组权重和运动机制全部公开。正式测试隐藏的是具体种子和生成的场景，不临时引入未声明的新规则。公开测试与私有测试的种子不重合；精英方案选择和调参只使用训练及公开测试，私有测试保持封存。

### 17.2 分数尺度

评分版本 `coverage-score/1.0`（`configs/scoring-v1.yaml`）。每组预先冻结随机策略平均回报 b_k 和学习基准平均回报 a_k，定义：

`S = 100 * Σ_k w_k * (mean(R_k) - b_k) / (a_k - b_k)`，且 `Σ_k w_k = 1`。

v1 配置为两组各权重 0.5，锚点 b_k = 0.0、a_k = 1.0。锚点从赛前独立校准产生并公布，不使用参赛者成绩动态更新，也不从私有最终测试重新计算。

三基线正式运行成绩（2026-09-19，逐案明细与判读见 `docs/baselines-anchor-draft.md`）：

| 基线 | 定位                  | performance_score |
| ---- | --------------------- | ----------------- |
| P901 | random（随机）        | 58.33             |
| P902 | rule（规则）          | 175.00            |
| P903 | learning（1M 步 PPO） | 116.67            |

random 与 rule 拉开约 3 倍差距，任务区分度的下限成立。

若某组 a_k 与 b_k 太接近，必须在赛前重新设计该组或采用其他稳定尺度，不能用极小分母放大随机波动。权重和锚点均属于评分版本内容。

所有参赛者使用相同的场景种子和策略重复采样安排；保存逐回合原始记录。报告每组均值和按场景聚合的不确定性区间。排名过近时按照赛前公布的统一补测规则处理，不能事后挑选有利种子。

### 17.3 选拔成绩

先设置可复现、接口合格、交付完整的资格门槛，再发布三列：核验性能分、实验质量等级、工程质量等级。算法榜按核验性能排序；录取时如何使用后两项，在开赛规则中单独公布，不写进策略协议。

实验与工程可各设 0 至 5 个明确检查项，避免按提交次数、代码行数或日志长度加分。例如实验检查对照、变量控制、重复实验、结论证据、失败分析；工程检查可运行入口、依赖锁定、模块组织、错误处理、复现说明。若组织方采用加权总分，必须赛前公布权重和具体判定标准，并在结果中保留各分项。

## 18. 评测运行与结果文件

参赛者本地提交预检：

```bash
python scripts/check_submission.py --submission participant/P0XX --output <输出目录>
```

组织方批量评分（每份提交在独立容器内评测，逐份隔离失败）：

```bash
python scripts/score_all.py \
  --submissions <提交根目录> \
  --suite configs/public-suite-v1.yaml \
  --seeds configs/public-seeds-v1.json \
  --image coverage-bench:verify-v1 \
  --output <输出目录>
```

批量脚本只枚举、调用和汇总；评分公式只有一处实现。单个提交失败不得中断其他人的评分。

正式评测顺序：解析冻结提交 → 校验协议与模型摘要 → 静态审计（禁不安全反序列化等）→ 准备独立推理依赖 → 容器内加载策略 → 执行协议检查 → 运行全部测试 → 从原始指标算分 → 写出结构化结果。

可信评测器持有环境与测试集；策略运行进程仅收到合法上下文和局部观测。正式执行采用独立工作进程，禁止网络访问和跨机器人共享可变文件，各进程只读必要策略代码及模型、只写自己的临时目录。

时间限制分为初始化、单步决策、单回合及整个任务四层（`coverage_bench/protocol.py` 的 `ResourceLimits`）；内存和模型体积限制独立配置。推理统一在中央处理器上进行。训练耗时作为实验信息记录，不作为不同个人硬件之间的直接扣分依据。

协议错误、推理超时、加载失败必须输出明确状态。正式有效分要求完整跑完规定测试；不能删去失败回合后对剩余成绩求平均。参赛者实现问题标记为提交无效，保留诊断结果但不计正式分；基础设施故障按预先定义的重试规则重新运行。

结果文件（`result.json`，结构见 `coverage_bench/results.py`）至少包含：结果格式版本、参赛编号、状态、协议/任务/评分三个独立版本、套件标识与摘要、运行条件、分组指标、`performance_score`、逐回合记录位置与错误类别。配套输出 `episodes.csv`（逐回合数据）与 `timings.npz`（耗时分布），均带内容摘要。

完整核验记录由组织方保存；对外过程报分只输出必要摘要。私有测试种子不随中途诊断传给参赛策略，赛后是否公开全部测试按赛前规则执行。

## 19. 实验记录、编号和公开授权

`LOG.md` 每次重要实验只需要说明：问题或假设、改变了什么、如何测试、结果和下一步。`experiments.csv` 建议记录实验编号、代码标识、训练配置摘要、训练种子、训练步数、模型摘要、公开测试版本、各组指标和备注。代码提交记录不能独自证明实验发生时间，也不能代替实验数据。

`REPORT.md` 说明最终策略、训练流程、关键对照、失败场景、资源使用和复现命令。原始训练日志可以保存为模型产物，不要求把全部中间模型塞进官方仓库。

编号公开、姓名映射由组织方私下保存。但比赛仓库仍会显示账号、提交作者与邮箱，编号只实现成绩展示的去实名，不能承诺彻底匿名。开赛时提供作者信息与敏感文件检查说明，避免赛后重写历史破坏冻结提交。

原创代码采用 MIT（宽松开源许可证）可以作为事先明确的参赛公开约定。提交时应明确确认本人可授权代码的公开许可，第三方代码保留原许可证和署名。将沉默直接视为完整授权不适合作为项目的默认操作规则。代码公开后第三方可能已经保存副本，移除官方索引不能保证所有副本消失。

评测漏洞的上报与认定规则见 `docs/vulnerability-disclosure.md`。

## 20. 扩展性验收

| 参赛者希望做的改动                   | 是否修改官方接口   | 所需支持                     |
| ------------------------------------ | ------------------ | ---------------------------- |
| 更换强化学习算法或优化器             | 否                 | 自由训练器，统一环境采样     |
| 更换普通网络、注意力网络或图网络     | 否                 | 实体表、稳定编号、有效标记   |
| 增加递归记忆或历史窗口               | 否                 | 每机器人实例、明确回合重置   |
| 使用集中价值网络                     | 否                 | 训练专用全局状态             |
| 参数共享或不同角色策略               | 否                 | 本机器人编号及独立加载状态   |
| 规则与学习组合                       | 否                 | 只需满足局部信息和动作限制   |
| 改训练奖励、课程或数据采样           | 否                 | 自己的训练包装与记录         |
| 从 3 台扩展到已声明容量以内          | 否                 | 固定容量和实际存在标记       |
| 更换已声明范围内的目标轨迹和视野     | 否                 | 官方场景配置与统一字段语义   |
| 增加摄像头图像、通信动作或新动作维度 | 是，进入新主版本   | 属于规则和信息权限变化       |
| 超过首届容量、引入回合内机器人失效   | 需要重新定义与验证 | 不能只加一个配置键就声称兼容 |

不使用任意字典字段向正式策略临时传隐藏信息。新增可选能力需要明确字段、权限、默认行为和版本，不能以"可扩展"为由留下不受约束的数据通道。

版本需要分别管理：

| 版本       | 变更范围                               | 兼容约束                                                                   |
| ---------- | -------------------------------------- | -------------------------------------------------------------------------- |
| 协议版本   | 字段、形状、归一化、动作含义、生命周期 | 删除字段或改变语义须升级主版本；未知可选元数据只能出现在明确允许的元数据区 |
| 任务版本   | 物理、目标运动、可见性和覆盖规则       | 输入形状相同也可能导致旧模型失效，不能只检查协议版本                       |
| 评分版本   | 指标聚合、分组权重和归一化锚点         | 结果必须携带此版本，不同版本的分数不得直接混排                             |
| 测试集版本 | 场景实例及种子安排                     | 相同任务下不同套件分别标识，公开与私有结果分列                             |

开赛后若修复影响行为的错误，官方统一公告，提供固定的新版本并安排所有受影响提交重测；不通过悄悄修改默认参数改变排名。

## 21. 官方基线与验收状态

官方发布材料包含：

1. 随机策略基线（`baselines/P901`）：证明安装、加载、重置、动作和输出路径可用。
2. 合法局部规则策略基线（`baselines/P902`）：用相同观测条件检验任务是否容易被简单追踪或分工方法做满。
3. 学习基准（`baselines/P903`）：提供训练配置、种子、模型、公开测试成绩和可复现命令（SB3 PPO，1M 步 CPU 训练约 3 分钟）；证明学生能够在公布的资源预算内完成一个有效学习循环。

三基线正式成绩见第 17.2 节与 `docs/baselines-anchor-draft.md`。

主要验收状态（验收需求清单 `tests/acceptance/requirements-v1.json`，共 69 条，随附运行手册 `docs/acceptance/`）：

- 同一固定版本与种子可重建场景；渲染开关不改变物理结果。
- 所有正式规模、视野和目标轨迹配置都产生符合规格的观测。
- 改动当前视野外的实体状态时，合法当前观测不发生不应有的变化。
- 不可见状态通过全局状态、附加信息、配置文件或共享内存泄漏的路径受到控制。
- 相邻目标、多个机器人争抢目标、两台机器人碰撞等小场景的指标有手工可核对的答案。
- 相同团队奖励不会按机器人数量重复累计。
- 回合重置清空记忆；调换回合顺序不应因残留状态改变结果。
- 单步、回合和任务超时能被终止并形成结构化错误，不阻塞整批评测。
- 非法动作和失败回合不会被悄悄修复、删除或少计。
- 更换网络、使用带记忆的策略、改变合法规模不需要修改官方协议。
- 本地公开评测与官方对同一公开套件的复核在公布容差内一致。
- 模型、依赖、评分版本或提交变更可以从结果中被识别。
- 简单规则、学习基准与经过改进的策略具有可测的性能差异。

## 22. 实施状态与冻结清单

| 阶段          | 交付物                                     | 状态                                   |
| ------------- | ------------------------------------------ | -------------------------------------- |
| A：规则与协议 | 任务文本、字段表、方法签名、版本规则       | 完成                                   |
| B：环境与指标 | 动态目标场景、观测、物理适配、奖励         | 完成                                   |
| C：加载与评测 | 工作进程、单人评分、结果文件、预检         | 完成                                   |
| D：基准与校准 | 随机、规则和学习基准、耗时与分差报告       | 完成（2026-09-19 正式运行）            |
| E：开赛冻结   | 固定版本、公开套件、封存私有套件、提交规则 | 部分完成（私有套件封存与正式发布待办） |

已冻结的数值以 `configs/` 为准：容量 A=B=8、场地半宽、时间步长、回合时长、半径、驱动力、阻尼、最大速度、边界行为、目标运动分布、感知范围、碰撞权重 λ=0.2、难度权重、分数锚点、评测局数、策略重复次数、推理资源限制、有效误差容差（`configs/reproduction-tolerance.json`）、近分补测阈值。提交时间与时区待赛前公布。
